<Application x:Class="WeekNumber.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WeekNumber"
             xmlns:vm="clr-namespace:WeekNumber.ViewModels"
             xmlns:tb="http://www.hardcodet.net/taskbar" xmlns:iconpacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!--  MahApps.Metro resource dictionaries. Make sure that all file names are Case Sensitive!  -->
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
                <!--  Theme setting  -->
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Themes/Light.Blue.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <vm:ViewModelLocator x:Key="ViewModelLocator" />

            <tb:TaskbarIcon x:Key="TrayIcon" ToolTipText="{Binding TrayText}">

                <tb:TaskbarIcon.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Viel Spaß">
                            <MenuItem.Icon>
                                <iconpacks:PackIconMaterial Kind="Heart" Height="16" Width="16" />
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Command="{Binding OpenCommand}" Header="Settings">
                            <MenuItem.Icon>
                                <iconpacks:PackIconMaterial Kind="CogBox" Height="16" Width="16" />
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Command="{Binding ExitCommand}" Header="Exit" >
                            <MenuItem.Icon>
                                <iconpacks:PackIconMaterial Kind="ExitToApp" Height="16" Width="16" />
                            </MenuItem.Icon>
                        </MenuItem>
                    </ContextMenu>
                </tb:TaskbarIcon.ContextMenu>

            </tb:TaskbarIcon>
        </ResourceDictionary>


    </Application.Resources>
</Application>
