<mah:MetroWindow x:Class="WeekNumber.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:iconpacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                 xmlns:local="clr-namespace:WeekNumber"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 Title="MainWindow"
                 Width="320"
                 Height="220"
                 MinWidth="320"
                 MinHeight="240"
                 MaxWidth="480"
                 MaxHeight="330"
                 Icon="{Binding Path=WeekIconImageSource}"
                 d:DataContext="{Binding Path=TrayViewModel, Source={StaticResource ViewModelLocator}}"
                 DataContext="{Binding Path=TrayViewModel, Source={StaticResource ViewModelLocator}}"
                 ShowCloseButton="False"
                 ShowMaxRestoreButton="false"
                 ShowMinButton="False"
                 ShowTitleBar="False"
                 mc:Ignorable="d">



    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Label HorizontalAlignment="Center"
                   Content="WeekNumber Einstellungen"
                   FontWeight="Bold" FontSize="{StaticResource MahApps.Font.Size.Window.Title}"/>
        </Grid>

        <Grid Grid.Row="2">

            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="8" />
                <RowDefinition />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0"
                   Grid.Column="0"
                   Content="Vordergrund:" />
            <mah:ColorPicker Grid.Row="0"
                             Grid.Column="2"
                             SelectedColor="{Binding Path=SelectedForegroundColor, Mode=TwoWay}" />

            <Label Grid.Row="2"
                   Grid.Column="0"
                   Content="Hintergrund:" />
            <mah:ColorPicker Grid.Row="2"
                             Grid.Column="2"
                             SelectedColor="{Binding Path=SelectedBackgroundColor, Mode=TwoWay}" />
        </Grid>

        <Grid Grid.Row="4">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="8" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Label HorizontalAlignment="Center"
                   Content="Vorschau"
                   FontWeight="Bold" />

            <Image Grid.Row="2"
                   MinWidth="28"
                   MinHeight="28"
                   Source="{Binding Path=WeekIconImageSource}" />
        </Grid>

        <Button Grid.Row="6"
                Command="{Binding Path=CloseCommand}"
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                Content="Schließen" />
    </Grid>

</mah:MetroWindow>
